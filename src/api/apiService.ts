import axios from 'axios';
import {Route} from "../store/routesSlice";

// export const getRouteFromAPI = async (routeId: string): Promise<Route> => {
    // try {
    //     // Преобразуем координаты в строку вида "lon1,lat1;lon2,lat2;lon3,lat3"
    //     const coordsString = coordinates.map(coord => coord.join(',')).join(';');
    //     const response = await axios.get(`http://router.project-osrm.org/route/v1/driving/${coordsString}?overview=full`);
    //     return response.data;
    // } catch (error) {
    //     throw error;
    // }
// };
